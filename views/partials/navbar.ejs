<nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
    <button class="btn btn-primary" id="menu-toggle">Toggle Menu</button>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="mx-5" style="width: 40rem;">
      <div class="input-group">
        <input type="text" class="form-control" placeholder="Search for...">
        <span class="input-group-append">
            <button class="btn btn-secondary" type="button">Go!</button>
        </span>
      </div>
    </div>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
        <!-- li class="nav-item <//% if (typeof active != 'undefined') { %>
            <//% if (active == 'Create'){ %>
                <//%= "active" %>  
               <//% } %>
            <//% } %>">
          <a class="nav-link" href='/create'>Create<span class="sr-only">(current)</span></a>
        </li -->
        <% if (typeof user != 'undefined' ) { %>
          <li class="nav-item mx-2">
            <a href="/cart">
            <button type="button" class="btn btn-primary">
              Cart
                <% if (typeof cartItems != 'undefined') { %>
                  <% if (cartItems.length > 0) { %>
                    <span class="badge badge-light ml-2">
                      <%= cartItems.length %> 
                    </span>
                  <% } %>
                <% } %>
            </button>
          </a>
          </li>
          <li class="nav-item dropdown" >
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= user.name.split(" ",1) %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <a class="dropdown-item" href="/user/<%=// username %>">Profile</a>
              <% if (user.accType == "seller") { %>
                <a class="dropdown-item" href='/create'>Create<span class="sr-only">(current)</span></a>
              <% } %>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="/logout">Logout</a>
            </div>
          </li>
        <% } else { %>
          <li class="nav-item mx-2">
            <a type="button" class="btn btn-primary" href="/login">
              Login
            </a>
          </li>
          <li class="nav-item mx-2">
            <a type="button" class="btn btn-primary" href="/register" >
              Register
            </A>
          </li>          
        <% } %>

        
      </ul>
    </div>
  </nav>